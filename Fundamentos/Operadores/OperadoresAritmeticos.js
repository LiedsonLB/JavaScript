// Operadores Aritmeticos

var x = 10
var y = 15

// Adição
var soma = 2 + 2; // 4
console.log(x + y);

// Substração
var substração = 2 - 2; // 0
console.log(x - y);

// Multiplicação
var multiplicação = 2 * 2; // 4
console.log(x * y);

// Divisão
var divisão = 2 / 2; // 1
console.log(x / y);